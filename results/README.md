# Experiment outputs

Generated reports, checkpoints and plots are ignored by Git. Use a separate
output directory for each parameter comparison; rerunning the same map, seed
and algorithm in one directory replaces matching files.

Reports include local source paths. Review reports before publishing them.
Checkpoints currently store learned tables, not full resumable training state.
